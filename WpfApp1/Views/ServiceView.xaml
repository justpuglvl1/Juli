<Window x:Class="WpfApp1.Views.ServiceView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1.Views"
        mc:Ignorable="d"
        Title="ServiceView" Height="450" Width="800">
    <Grid>

        <Grid.RowDefinitions >
            <RowDefinition Height="30"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Button Width="150" Height="25" HorizontalAlignment="Left" Content="Открыть документ" Click="Button_Click"/>
        </Grid>
        
        <DataGrid Grid.Row="1" x:Name="dtg" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Имя" Width="*" Binding="{Binding Name}" IsReadOnly="True"/>
                <DataGridTextColumn Header="Стоимость" Width="*" Binding="{Binding Salary}" IsReadOnly="True"/>
                <DataGridTextColumn Header="Код" Width="*" Binding="{Binding ServiceCode}" IsReadOnly="True"/>
                <DataGridTextColumn Header="Срок исполнения" Width="*" Binding="{Binding Term}" IsReadOnly="True"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
